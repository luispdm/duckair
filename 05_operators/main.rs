fn main() {
    // math operators
    println!("{}", 18%7); // modulus operator, prints 4
    println!("{}, {}", i32::pow(8, 2), f32::powf(2.2, 2.0)); // 64 4.84
    // println!("{}", i32::pow(2.2, 3)); // compiler error, need to use f32::powf for this operation

    // logical operators & bitwise operators
    // same as in go
}
